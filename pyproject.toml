[project]
name = "ascent"
version = "0.1.0"
description = "A free, agent-agnostic AI engineering course with an AI mentor, spaced repetition and close progress tracking."
requires-python = ">=3.10"
license = { text = "Apache-2.0 (code) and CC-BY-SA-4.0 (content)" }

[project.urls]
Repository = "https://github.com/shubhampandey567/Ascent"

[tool.pytest.ini_options]
testpaths = ["tests"]
